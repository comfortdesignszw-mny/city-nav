import { TransporterProfile } from '../types';

/**
 * Production Clean: Initial empty state for Transporters Directory.
 * Ready for local kombi drivers, fleet owners, and operators to register.
 */
export const SEED_TRANSPORTERS: TransporterProfile[] = [];
